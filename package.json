{
    "name": "snake-showdown-live",
    "version": "1.0.0",
    "private": true,
    "description": "Real-time multiplayer snake game",
    "scripts": {
        "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
        "dev:backend": "cd backend/backend && uv run uvicorn main:app --reload",
        "dev:frontend": "cd frontend && npm run dev",
        "test": "concurrently -n backend,frontend -c blue,green \"npm run test:backend\" \"npm run test:frontend\"",
        "test:backend": "cd backend/backend && uv run pytest",
        "test:frontend": "cd frontend && npm test -- run"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}